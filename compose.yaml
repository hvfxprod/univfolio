services:
  web:
    build: .
    image: univfolio:local
    ports:
      - "${PORT:-8080}:80"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
