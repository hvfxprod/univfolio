# Optional host port for Docker Compose. No API keys are required.
PORT=8080

# Public HTTPS deployment: set the exact origin without a trailing slash.
# PUBLIC_ORIGIN=https://sandbox.pixelsurgery.cloud
# COOKIE_SECURE=true
# Local HTTP testing only:
COOKIE_SECURE=false
